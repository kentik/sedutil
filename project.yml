name: sedutil
version: TAG
target:
  build: 
docker:
  build: bash ManualBuild.sh
    base: kt-build-generic-bullseye:master
  run:
    base: kt-run-generic-bullseye:master
skip_debian: true
skip_docker: true
